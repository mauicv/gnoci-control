# gnoci

## Build and run dev

```
cmake --preset dev
cmake --build --preset dev -j
./build/dev/gnoci
```

## Build and run release

```
cmake --preset release
cmake --build --preset release -j
./build/release/gnoci
```



